// random number between 20 and 200
// Math.random() returns number between 0 and 1

Math.floor(20 + Math.random() * (200 - 20 + 1));

// The Math.random() function returns a floating-point, pseudo-random number in
// the range [0, 1) (inclusive of 0, but not 1) 包含0
